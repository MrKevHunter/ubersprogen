IF EXISTS (SELECT 1 FROM sys.objects WHERE object_id = OBJECT_ID(N'[$procConfig.Schema].[$procedureName]') AND type in (N'P', N'PC'))
BEGIN
	DROP PROCEDURE [$procConfig.Schema].[$procedureName]
END
GO